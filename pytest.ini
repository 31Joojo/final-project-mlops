[pytest]
testpaths = tests
pythonpath =
    .
    apps/api
markers =
    unit: fast unit tests
    integration: integration tests (API/TestClient)
    e2e: end-to-end tests hitting the live staging environment
addopts = -q